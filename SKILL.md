---
name: dosa
description: 정리된 자료(텍스트·노션·PDF·문서 드래프트 이미지 등)를 받아 디자인 시스템 가이드 따라 슬라이드 보고서 HTML로 변환. 도사가 페이지 분할·카드/표 선택·타이포 위계를 룰에 맞춰 자동 적용.
---

# /dosa — 문서 변환 도사

## 호출 시 흐름

### 1. 사용자 입력 확인
- 받을 수 있는 형태: 노션·구글독스 텍스트 한 덩어리 / PDF 파일 경로 / 문서 드래프트 이미지(스크린샷·손그림 등)
- PDF는 Read 툴로 직접 읽고, 이미지도 Read로 시각 인식 가능
- 입력이 없으면 "변환할 자료를 붙여넣거나 파일 경로를 주세요" 요청

### 2. 가이드 읽기 (필수, 매번)
본 스킬 폴더(`~/.claude/skills/dosa/`) 안의 두 문서를 읽는다:
- `dosa.md` — 변환 원칙 (페이지 분할·카드 vs 표·표 타이포·간지 사용 등)
- `design.md` — 디자인 시스템 정의 (컴포넌트 매핑·폰트·색상 토큰·라벨 표기)

두 문서를 모두 읽고 **이번 변환에 적용할 룰을 머릿속에 펼친 뒤** 변환 시작.

추가로 작업 중 함정에 부딪히면 `traps/` 폴더 안 관련 노트 펼쳐 볼 것:
- `traps/contenteditable.md` — 인라인 편집·IME·trailing 청소·캐시 함정
- `traps/export.md` — 굽기 / 내보내기 / 공유 모드 함정
- `traps/ix-block.md` — 이미지 블록(ix-block) 통합 함정
- `traps/dashboard-layout.md` — 슬라이드 탑/미드/봇 레이아웃·z-index·다크모드 토큰
- `traps/transition.md` — 리스트 추가/제거 트랜지션·element 상태 변동성

### 3. 작업 공간 셋업 확인 (첫 호출 시 1회만)

작업 공간 = 사용자가 만든 작업물(보고서 HTML들)이 쌓이는 폴더.

**기본 위치**: `~/Documents/DOSA` (또는 사용자가 지정)

**확인 절차**:
1. 작업 공간 폴더 존재 여부 확인
2. **없으면** — 사용자에게 한 줄 묻기: "작업물을 어디에 저장할까요? (기본: `~/Documents/DOSA`)"
3. 폴더 생성 후 **스킬의 `fonts/` 폴더를 작업 공간으로 복사**:
   ```
   cp -R ~/.claude/skills/dosa/fonts ~/Documents/DOSA/fonts
   ```
   - 이유: 작업물 HTML 안 폰트 경로가 `../fonts/` 상대 경로라 작업 공간에 fonts/ 사본 필요
   - 폰트는 거의 안 바뀌니 사본 drift 위험 낮음
4. **있으면** — fonts/ 사본도 있는지 확인. 없으면 위 cp 한 번 실행.
5. 작업 공간 위치를 `<DOSA-ROOT>` 변수로 저장 (이번 호출에서 계속 사용)

### 4. 작업물 폴더 결정
- 폴더명: 노션 내용에서 짧고 구체적인 이름 추출 (예: `mau-800m`, `q3-roadmap`)
- 사용자에게 한 줄 제안 후 OK 받기
- 위치: `<DOSA-ROOT>/<work-name>/` (평평하게. 작업물 5개 넘어가면 `archive/` 도입 검토)

### 5. 베이스 HTML cp + 슬라이드 교체
- **베이스 위치**: `~/.claude/skills/dosa/aqua-dashboard/dashboard.html` (표준 — xt 테이블 시스템 + popover + bullet 일반화 + ix-block 이미지 + 굽기/내보내기 export 등 모든 인터랙션 탑재)
- 사용자 작업물 폴더로 cp → `<DOSA-ROOT>/<work>/<work>.html`
- **localStorage 키 rename**: `new3-dashboard-` → `<work>-` 일괄 치환 (다른 작업물과 충돌 방지). `new3-dashboard-migrated-force-v1` 같은 마이그레이션 플래그도 같이 치환됨 — OK
- 슬라이드 wrapper 안 슬라이드 영역만 새 콘텐츠로 교체. 카운터(slideTotal)는 JS가 자동 계산
- 폰트 경로(`../fonts/`)는 그대로 — 작업 공간의 fonts/ 사본을 가리킴
- 이미지는 base64 data URL로 HTML에 직접 인라인 (외부 src 참조 X — 단일 파일로 self-contained 유지)

### 6. 변환 체크리스트 (저장 전)
- [ ] 노션 박스 1개 = 슬라이드 1개로 1:1 매핑하지 않았는가 (같은 위계 묶음 = 한 장)
- [ ] 빈약한 페이지(타이틀 + 짧은 bullets만) 없는가
- [ ] 시간/단계/비교가 한 장 가로 카드 그리드에 들어갔는가 (Q2/Q3/Q4 같은 패턴)
- [ ] 간지가 연속 2개로 들어가진 않았는가
- [ ] 표를 카드로 재구성 가능한데 표로 갔지는 않았는가 (`design.md` 12절 검토)
- [ ] 모든 표 위에 컨텍스트 라벨(`seg-track-label` 톤) 두었는가
- [ ] 핵심 행은 강조 클래스(`row-hl`)로 표시했는가
- [ ] 셀 폰트는 본문보다 작게 설정됐는가

### 7. 결과 보고
- 사용자에게 작업물 경로 + 슬라이드 수 + 사용한 컴포넌트 종류 요약
- 변환 중 판단한 결정(예: "박스 3개를 한 장 lc-cards로 압축", "ADI 표를 pat4 카드로 재구성")을 한두 줄 명시 — 사용자가 검토 후 의도와 다르면 그 페이지만 다시 만들 수 있도록

---

## 사용자가 결정해야 하는 영역 (확인 필수)

- 첫 호출 시 작업 공간 위치 (기본 `~/Documents/DOSA`)
- 작업물 폴더명 (한 줄 제안 후 OK 받기)
- 노션 원본의 의도된 비일관성(두 표기 공존 등) 통일 여부
- 슬라이드 개수 추가/삭제 결정

## 도사가 자유롭게 결정하는 영역

- 카드 수 / 레이아웃 패턴 (2열/3열/4열)
- 애니메이션 delay 분배
- 색상 강조 위치 (--accent 사용 빈도)
- bullet 강조 (`<li class="hl">`)
- 컴포넌트 선택 (`bh2` vs `lc-cards` 등 — `design.md` 12절 매핑 참고)

---

## 다른 디자인 톤 추가될 때

`~/.claude/skills/dosa/`에 새 가이드 파일(예: `design-light.md`) 추가하면 본 SKILL.md 손대지 않아도 됨. 호출 시 도사가 자동으로 인식하고 사용자에게 톤 선택지 제시.

---

**자세한 변환 룰은 절대 본 문서에 인라인으로 적지 말 것.** 룰의 단일 출처는 `dosa.md` + `design.md` (본 스킬 폴더 안). 룰 변경 시 그쪽만 갱신.

