# DOSA 이용 가이드

도사는 사용자가 정리한 보고서/리포트 텍스트를 디자인 시스템 따라 슬라이드 보고서 HTML로 변환하는 도구다.

---

## 1. 시작하기

### 호출
Claude Code에서 슬래시 명령으로 호출:

```
/dosa
<여기에 보고서/리포트 내용 붙여넣기>
```

또는 그냥 `/dosa` 만 호출하면 도사가 "변환할 내용을 붙여넣어 주세요"라고 묻는다.

### 입력 가능한 형식
보고서·리포트·기획서 같은 정리된 텍스트면 무엇이든 OK — 노션 복붙, 구글독스, 마크다운, 한글 등. 구조(제목/박스/표/리스트)가 명확할수록 결과가 좋다.

### 처음이라면
도사는 두 문서를 매번 자동으로 읽는다 (스킬 폴더 안):
- `~/.claude/skills/dosa/dosa.md` — 어떻게 페이지를 자르고 카드/표를 고를지 (변환 원칙)
- `~/.claude/skills/dosa/design.md` — 컴포넌트·폰트·색상 (디자인 시스템)

룰을 미리 읽고 싶으면 두 파일 직접 보면 된다. (Finder에서 `.claude` 폴더는 숨김이라 **Cmd+Shift+.** 로 표시 토글)

---

## 2. 첫 호출 셋업 (자동)

도사를 처음 호출하면 자동으로 작업 공간을 셋업한다:

1. **작업 공간 위치 확인** — 도사가 한 줄 묻기: "작업물을 어디에 저장할까요? (기본: `~/Documents/DOSA`)"
2. **폴더 생성 + 폰트 복사** — 작업 공간이 없으면 만들고, 스킬의 `fonts/`를 작업 공간으로 복사
   - 이유: 작업물 HTML 안 폰트 경로가 `../fonts/` 상대 경로라서
3. 이후 호출부터는 셋업 단계 건너뜀

수동으로 셋업하려면:
```bash
mkdir -p ~/Documents/DOSA
cp -R ~/.claude/skills/dosa/fonts ~/Documents/DOSA/fonts
```

---

## 3. 도사가 묻는 것 / 사용자가 결정하는 것

도사는 변환 도중 다음을 사용자에게 확인한다:

1. **(첫 호출만) 작업 공간 위치** — 기본 `~/Documents/DOSA`
2. **작업물 폴더명** — 입력 내용에서 짧게 추출 (예: `mau-800m`, `q3-roadmap`). 도사가 한 줄 제안하면 OK 또는 변경
3. **의도된 비일관성 통일 여부** — 원본에 두 가지 표기가 공존하면 어느 쪽으로 갈지
4. **슬라이드 개수 추가/삭제** — 도사가 "이 섹션은 1장으로 압축했어요" 같은 결정을 보고하면 검토

도사가 자유롭게 결정하는 영역:
- 카드 수 / 그리드 레이아웃 (2열 vs 3열)
- 컴포넌트 선택 (`bh2` vs `lc-cards` vs `pat4` 등)
- 색상 강조 위치, 애니메이션 delay 분배

---

## 4. 결과 확인

도사가 변환을 끝내면:
- 작업물 폴더: `<DOSA-ROOT>/<work-name>/`
- 결과 HTML: `<DOSA-ROOT>/<work-name>/<work-name>.html`
- 변환 결정 요약 (어떤 박스를 어떤 카드로 압축했는지)

**확인 방법**: 결과 HTML을 새 탭으로 열기 (수정 후엔 반드시 새 탭, 캐시 문제 있음).

브라우저에서 확인할 수 있는 것:
- `←` `→` 또는 스페이스로 슬라이드 이동
- `↑` 오버뷰 패널 (썸네일)
- `T` 다크/라이트 토글
- 텍스트 클릭 → 인라인 편집
- `Cmd+↑/↓` 폰트 크기 조절
- `Cmd+B` 굵게

---

## 5. 수정 요청

변환 결과가 마음에 안 들면 도사에게 자연어로 말한다:

```
3페이지 카드 3개 말고 표로 바꿔줘
```

```
Q2/Q3/Q4를 한 페이지로 합쳐
```

```
표지 부제 더 짧게
```

도사가 해당 페이지만 다시 만들어 결과 HTML에 반영한다.

---

## 6. 인라인 편집 (브라우저)

생성된 HTML은 인라인 편집 가능 — 작업 중인 보고서를 브라우저에서 직접 수정.

- 텍스트 클릭 → 편집 모드 (그린 테두리)
- 텍스트 **선택** → 색상 팝오버 (부분 색상 적용)
- `Cmd+Enter` 저장 종료
- `Esc` 취소
- `Cmd+B` 굵게

수정 사항은 localStorage에 자동 저장. 다른 브라우저에서 열면 반영 안 됨 — HTML 내보내기로 영구 저장.

---

## 7. 자주 묻는 것

**Q. 어떤 형식으로 입력하면 돼?**
A. 보고서·리포트·기획서 같은 정리된 텍스트면 무엇이든. 노션 복붙, 구글독스, 마크다운, 한글, 그냥 텍스트 다 가능. 구조(제목/박스/표)가 명확할수록 결과가 좋다.

**Q. 이미지는 어떻게?**
A. 도사는 이미지 placeholder만 넣음. 실제 이미지는 작업물 폴더의 `imgs/`에 넣고 HTML에서 경로 교체. 또는 도사에게 "5페이지 이미지를 imgs/foo.png로 바꿔줘"라고 요청.

**Q. 다른 톤(밝은 톤, 미니멀 등)으로도 만들 수 있어?**
A. 현재는 design.md 한 가지 톤만 있다. 새 톤 필요하면 도사에게 "새 톤 추가하자"고 하면 새 가이드 파일 만들고 첫 작업물부터 톤 확립.

**Q. 작업물이 많아지면?**
A. 5개 이상 누적되면 도사가 `archive/` 폴더로 묶을지 제안한다.

**Q. 잘못 만들었으면 어떻게 지워?**
A. 작업물 폴더 통째로 삭제하면 된다 (`<DOSA-ROOT>/<work-name>/` rm). 다른 작업물 영향 없음 — localStorage 키도 작업물별로 분리되어 있다.

**Q. 폰트는 어디 있어?**
A. 마스터는 스킬 폴더 (`~/.claude/skills/dosa/fonts/`). 사본은 작업 공간에 도사가 자동 복사 (`<DOSA-ROOT>/fonts/`). 작업물은 사본을 참조한다.

---

## 8. 폴더 구조

도사는 두 위치를 사용한다:

**스킬 폴더** (`~/.claude/skills/dosa/`) — 변환 룰·가이드·자산. Finder 숨김 폴더 (Cmd+Shift+. 로 토글):
```
~/.claude/skills/dosa/
├── SKILL.md           # 스킬 본체 (호출 시 자동 로드)
├── guide.md           # 본 문서 (이용 가이드)
├── dosa.md            # 변환 원칙
├── design.md          # 디자인 시스템 가이드
├── fonts/             # 표준 폰트 마스터 (You&I + SF Pro)
└── aqua-dashboard/    # 베이스 HTML + 학습 산출물 (참고)
```

**작업 공간** (예: `~/Documents/DOSA/`) — 사용자가 만든 작업물 누적:
```
<DOSA-ROOT>/
├── fonts/             # fonts 사본 (도사가 첫 호출 시 자동 복사)
└── <work-name>/       # 작업물 폴더 (변환 결과)
    ├── <work>.html
    └── imgs/
```

각 파일 역할:
- **SKILL.md** — `/dosa` 호출 시 자동 로드되는 행동 지침 (짧음)
- **guide.md** — 사용자 이용 가이드. 본 문서
- **dosa.md** — 변환 원칙 (페이지 분할·카드 vs 표·간지 등 디자인 무관 룰)
- **design.md** — 디자인 시스템 정의 (컴포넌트·색상·폰트·라벨 표기)

작업물 폴더는 평평하게 시작. 5개 이상 누적되면 그때 `archive/`로 묶는다.

---

## 9. 표준 폰트

도사로 만든 모든 문서는 두 폰트를 표준으로 사용:

- **YouandiNewKrTitle (You&I)** — 한국어 큰 강조·제목
- **SF Pro Display** — 영문·숫자·본문·UI

폰트 마스터는 스킬 폴더(`~/.claude/skills/dosa/fonts/`)에. 작업 공간에 사본 두고 작업물 HTML이 `../fonts/` 상대 경로로 참조.

새 디자인 톤을 만들어도 위 두 폰트 베이스 권장.

---

## 10. 새 디자인 톤 추가하기

현재는 design.md 한 가지 톤뿐. 다른 톤(밝은 미니멀, 키노트 풍 등)이 필요하면:

1. 도사에게 "새 디자인 톤을 만들고 싶어. <원하는 분위기>" 요청
2. 도사가 새 작업물부터 톤 확립 (CSS 토큰·컴포넌트·폰트 결정)
3. 안정화되면 `~/.claude/skills/dosa/<new-name>.md` 가이드 파일 생성 (예: `design-light.md`)
4. 이후 호출 시 도사가 톤 선택지 제시

---

## 11. 다환경 동기화 (집 / 회사)

도사는 `choongsholic/dosa` GitHub 레포로 추적되어 여러 컴퓨터 간 동기화한다. 한쪽에서 가이드 수정하거나 새 톤 추가하면 다른 쪽에서 풀로 받아오면 됨.

**트리거** (어느 working dir에서나):
- **"도사 푸시"** → 변경사항 add+commit+push
- **"도사 풀"** → 다른 환경에서 변경분 받아오기

**새 컴퓨터 첫 셋업**:
1. 터미널에서 `git clone https://github.com/choongsholic/dosa ~/.claude/skills/dosa`
2. Claude Code에서 `/dosa` 호출 — 첫 호출 시 도사가 작업 공간 위치 묻고 fonts/ 자동 복사

스킬 폴더 자체에 룰·가이드·폰트·베이스 HTML이 다 들어있어서 추가로 받을 게 없다.

**작업물(`<DOSA-ROOT>/<work-name>/`)은 동기화 X** — 본인 컴퓨터에만 살아남. 동기화되는 건 스킬 자체(룰·디자인 가이드·자산)만.
